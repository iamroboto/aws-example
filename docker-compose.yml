services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws_interview_backend
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env
    extra_hosts:
      - 'host.docker.internal:host-gateway'
